import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rossi.projectmobile',
  appName: 'Rossi Mobile',
  webDir: 'dist-gui'
};

export default config;
